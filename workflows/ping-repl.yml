name: Ping Repl Keep-Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Afficher l heure UTC
        run: date -u
      - name: Ping le endpoint Repl
        run: |
          echo "Ping de maintien en vie du Repl..."
          if curl -fsS https://310687d5-bac5-4343-af25-d2535d08e02b-00-1cknirvdbm20s.spock.replit.dev/ping; then
            echo "OK"
          else
            echo "Échec du ping" >&2
            exit 1
          fi
